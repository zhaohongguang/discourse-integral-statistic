{{#d-modal-body class="create-theme-modal" title="admin.customize.theme.create"}}
  <div class="input">
    <span class="label">
      {{I18n "admin.rule.reason"}}
    </span>
    <span class="control">
      {{combo-box valueAttribute="value" content=types value=selectedType}}
    </span>
  </div>

  <div class="input">
    <span class="label">
      {{I18n "admin.rule.note"}}
    </span>
    <span class="control">
      {{input value=name placeholder=placeholder}}
    </span>
  </div>

  {{!-- {{#if showError}}
    <div class="error">
      {{d-icon "warning"}}
      {{I18n "admin.customize.theme.name_too_short"}}
    </div>
  {{/if}} --}}
{{/d-modal-body}}

<div class="modal-footer">
  {{d-button class="btn btn-primary" label="admin.customize.theme.create" action="createRecord" disabled=saving}}
  {{d-modal-cancel close=(action "closeModal")}}
</div>
